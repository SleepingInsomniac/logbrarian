require "spec_helper"

describe Logbrarian do
  it "has a version number" do
    expect(Logbrarian::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
